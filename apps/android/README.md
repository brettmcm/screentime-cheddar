# Cheddar Android

This directory is reserved for the native Android implementation using Kotlin
and Jetpack Compose.

The Android app should consume the shared Cheddar design tokens and match the
same Figma app flow as the native iOS and React web implementations.
